services:
  app:
    build: .
    ports:
      - "8888:8888"
    environment:
      APP_PORT: "8080"
      APP_ENV: "prod"
      JWT_SECRET: "super-secret-key-change-me"
      DB_PATH: "/app/data/app.db"
    volumes:
      - ./data:/app/data
      - ./certs:/app/certs
